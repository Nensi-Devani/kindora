import 'package:flutter/material.dart';

class AppColors {
  // Primary button color
  static const Color primaryButton = Color(0xFFB55266);

  // Light background color (app base background)
  static const Color lightBackground = Color(0xFFF7E6CD);

  // Secondary button / dark background
  static const Color secondaryBackground = Color(0xFFE7AC98);

  // Accent background (search, widgets etc.)
  static const Color accentBackground = Color(0xFFECCABF);
}
